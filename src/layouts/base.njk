<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="canonical" href={{ page.url | url | absoluteUrl(site.url) }}>
  {% bundledcss %}
    <link href="https://pvinis.github.io/iosevka-webfont/3.4.1/iosevka.css" rel="stylesheet" />
    <title>
        Homebrew | Hackerspace PESUECC    
    </title>

    <link rel="icon" type= “image/x-icon” href="/images/nav-logo.png">

    <meta property="og:title" content="{{  title or site.name | escape }}">
    <meta property="og:site_name" content="Homebrew | Hackerspace PESUECC"/>
    <meta property="og:type" content="website"/>
    <meta property="og:url" content="{{ site.url + page.url }}"/>
    <meta name="twitter:card" content="summary_large_image">

    {% if site.author.twitterHandle %}
      <meta name="twitter:creator" content="@{{ site.author.twitterHandle | replace('@', '') }}"/>
    {% endif %}

    {% set desc = description %}
    {% if not desc %}
      {% set desc = content | striptags | truncate(140) %}
    {% endif %}
    {% if desc %}
      <meta name="description" content="{{ desc | escape  }}">
      <meta property="og:description" content="{{ desc | escape }}">
      <meta name="description" content="{{ desc | escape }}"/>
    {% endif %}

    {% if not socialImage %}
      {% set socialImage = site.defaultSocialImage %}
    {% endif %}
    {% if socialImage %}
      <meta property="og:image" content="{{ socialImage | url | absoluteUrl(site.url) }}"/>
      <meta name="twitter:image" content="{{ socialImage | url | absoluteUrl(site.url) }}"/>
    {% endif %}

    {# Add mermaid -- start #}
      <script src="https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.min.js"></script>
      <script>mermaid.initialize({startOnLoad:true});</script>
    {# Add mermaid -- end #}
  </head>
  <body>
    <div class="layout-wrapper">

      <header class="header">
        <div class="header__content">

          {% if site.headerLinks %}
          <nav class="navbar">

            <div class="navbar-header" >
              <a href="/"> 
              <img class="navbar-header-image" href="/" src="/images/nav-logo.png" alt="">
              </a>
            </div>

            <div class="navbar-body">
              <div class="navbar-body-content"> 
                {% for item in site.headerLinks %}
                {% set currentAttribute = '' %}
                {% set externalAttribute = '' %}

                {% if item.external %}
                {% set externalAttribute = 'target="_blank" rel="noopener noreferrer"' %}
                {% endif %}

                {% if page.url == item.url %}
                {% set currentAttribute = 'aria-current="page"' %}
                {% endif %}

                <a href="{{ item.url | url }}" class="nav-link" {{ currentAttribute | safe }} {{ externalAttribute | safe }}>{{ item.text }}</a>
                {% endfor %}
              </div>
            </div>
          </nav>

          {% endif %}

        </div>
      </header>

      <main class="main">
        {{ content | safe }}
      </main>

      <footer class="container mx-auto">
        <div class="footer__content">

          <!-- Social and Webring Section -->
          <div class="footer-links-section">
            <div class="footer_flex">
            {% if site.socialLinks %}
              {% for item in site.socialLinks %}
                {% set externalAttribute = '' %}

                {% if item.external %}
                  {% set externalAttribute = 'target="_blank" rel="noopener noreferrer"' %}
                {% endif %}

                <li>
                  <a href="{{ item.url | url }}" {{ externalAttribute | safe }} >{{ item.text }}</a>
                </li>
              {% endfor %}
            {% endif %}

            {% if site.footerLinks %}

              {% for item in site.footerLinks %}
                {% set externalAttribute = '' %}

                {% if item.external %}
                  {% set externalAttribute = 'target="_blank" rel="noopener noreferrer"' %}
                {% endif %}

                <li>
                  <a href="{{ item.url | url }}" {{ externalAttribute | safe }} >{{ item.text }}</a>
                </li>
              {% endfor %}
            {% endif %}
            </div>
          </div>

          <!-- Homebrew Webring -->
          <div class="webring-section">
            <div class="webring-header">
              <h3><a href="/webring">Homebrew Webring</a></h3>
            </div>
            <div class="webring-nav">
              <a href="/ring/prev?site=https://homebrew.hsp-ec.xyz" class="webring-link">← Previous</a>
              <a href="/ring/random?site=https://homebrew.hsp-ec.xyz" class="webring-link">🔀 Random</a>
              <a href="/ring/next?site=https://homebrew.hsp-ec.xyz" class="webring-link">Next →</a>
            </div>
            <div class="webring-sites">
              <p class="webring-sites-label">Sites in the ring:</p>
              <ul class="webring-list">
              {% for site_item in sites.ring %}
                <li><a href="{{ site_item.url }}" target="_blank" rel="noopener noreferrer">{{ site_item.title }}</a></li>
              {% endfor %}
              </ul>
            </div>
          </div>

          {% if site.showFooterAttribution %}
          {% endif %}

        </div>
      </div>

    </footer>

    {% bundledjs %}
  </body>
</html>
